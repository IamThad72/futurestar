# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files (never commit - contains secrets)
.env
.env.*
!.env.example

# Database dumps (may contain sensitive data)
db-dump-*.sql

# Local Bible text dumps (public domain; keep SOURCE.txt)
data/bible/**/*.json
data/bible/**/*.sql
data/bible/**/*.csv

# USDA FoodData Central dumps (multi-GB; never commit zip/json)
data/usda/

# Capacitor / native build artifacts (never commit)
android/.gradle/
android/**/build/
android/local.properties
ios/App/App/public/
ios/App/Pods/
ios/DerivedData/
*.xcuserstate

.vercel
.env*

# Uploaded estate / financial documents (never commit binaries)
uploads/
